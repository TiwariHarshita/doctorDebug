# DebugPilot AWS patch

Start with `DEPLOYMENT_GUIDE.md`.

This patch was prepared from the supplied API `src`, Prisma schema/migrations, and demo app
server. The actual `apps/web` source and package files were not supplied, so the dashboard
button is intentionally isolated and receives the existing auth token as a prop.
