version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npx prisma generate
      - npm run build
    post-build:
      - npm prune --omit=dev
run:
  command: npm run start
  network:
    port: 5050
    env: PORT
